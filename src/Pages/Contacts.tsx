import * as React from "react";

export const Contacts: React.FC  = ()=> {

    return (
        <div style={{position: 'absolute', top: '120vh', left: 0, width: '100%'}}>
            <div style={{textAlign: "start", margin: 100}}>Contacts</div>
        </div>
    )
}